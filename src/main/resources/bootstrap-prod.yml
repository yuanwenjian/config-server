spring:
    application:
        name: config-server

    profiles:
        active: prod

    cloud:
        config:
            server:
                git:
                    uri: https://github.com/yuanwenjian/config-repo
